@model SCManager.ViewModels.Privacy.IndexViewModel

@{
    ViewData["Title"] = "Privacy";
}

<!-- Basic Card Example -->
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">About</h6>
    </div>
    <div class="card-body">
        @Html.Raw(Model.Content)
    </div>
</div>